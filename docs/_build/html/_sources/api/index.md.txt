# API reference

The API docs are split by module so each page can stay focused on one part of
the package. The goal is to document the public surface, not every helper that
exists to support the implementation.

```{toctree}
:maxdepth: 1

package
client
models
service
cli
```
